"dum" is an extremely small file web server. Intentional issues:
* No memory deallocation on exit
* If something goes wrong during file operations, 404 is sent instead of 500

Available on Docker Hub: https://hub.docker.com/r/megahomyak/dum/tags
